<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container-fluid">
    <img alt="Rails" src="/assets/ipay.png" />
      <% if signed_in? %>
      <nav>
        <ul class="nav pull-right">
        	<%unless is_admin?%>
            <li><%= link_to "Employee Info", empinfo_path %></li>
            <%end%>
            <li><%= link_to "Pay Slips", payslips_path %></li>
            
            <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                Declarations & Taxes <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "New DeclarationForm", emp_declaration_details_path(:emp_id => employee_view) %></li>
                <%unless is_admin?%>
            <li><%= link_to "Sign out", signout_path, :method=> "delete" %></li>
            <%end%>
        </ul>
      </nav>
      <%end%>
    </div>
  </div>
  
</header>

